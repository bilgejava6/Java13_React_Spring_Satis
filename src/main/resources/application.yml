server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/Java13Satis
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
